# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[templates]
defer = "{{ hooks?.pre | nl }}{% for file in files %}ensure_zcompiled \"{{ file }}\"\nzsh-defer source \"{{ file }}\"\n{% endfor %}{{ hooks?.post | nl }}"
zcompile = "{{ hooks?.pre | nl }}{% for file in files %}ensure_zcompiled \"{{ file }}\"\nsource \"{{ file }}\"\n{% endfor %}{{ hooks?.post | nl }}"

[plugins.dotfiles-main]
local = "~/dotfiles/zsh/conf.d/sync"
use = ["*.zsh"]

[plugins.zsh-defer]
github = "romkatv/zsh-defer"
apply = ["zcompile"]

[plugins.local-zfunc]
local = "~/.zfunc"
apply = ["fpath"]

[plugins.local-completion]
local = "~/.zsh/completion"
apply = ["fpath"]

[plugins.zsh-autosuggestions]
github = 'zsh-users/zsh-autosuggestions'
apply = ["defer"]

[plugins.fast-syntax-highlighting]
github = 'zdharma-continuum/fast-syntax-highlighting'
apply = ["defer"]

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"
apply = ["defer"]

[plugins.sdkman.hooks]
pre = "export SDKMAN_DIR=\"$HOME/.sdkman\""

[plugins.sdkman]
local = "~/.sdkman"
use = ["sdkman-init.sh"]
apply = ["defer"]

[plugins.sdkman-bin]
local = "~/.sdkman/bin"
apply = ["path"]

[plugins.rbenv-completion]
local = "~/.rbenv/completions"
apply = ["fpath"]

[plugins.zsh-abbr]
github = "olets/zsh-abbr"
use = ["zsh-abbr.plugin.zsh"]
apply = ["defer"]

[plugins.dotfiles-defers]
local = "~/dotfiles/zsh/conf.d/defer"
use = ["*.zsh"]
apply = ["defer"]
